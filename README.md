# gitops-platform
gitops-platform

### Documentation on how platform apps are deployed

Platform apps are defined once and fanned out via ApplicationSets.

Placement is driven by cluster metadata (config/clusters.yaml).

We can also filter by env, region, etc. if needed.
